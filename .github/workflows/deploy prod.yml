name: Deploy prod

on:
  push:
    branches:
      - main
  workflow_dispatch:
  
env:
  DOTNET_VERSION: "9.x"
  NUGET_PACKAGES_DIRECTORY: ..\..\_nuget\packages
  PROJECT: ConsoleApp1\ConsoleApp1.csproj
  TEST_PROJECT: TestProject1\TestProject1.csproj

jobs:
  build:
    runs-on: [self-hosted, windows]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          clean: true
    
      - name: Setup dotnet
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}
        env:
          DOTNET_INSTALL_DIR: ${{ runner.tool_cache }}\dotnet

      - name: Restore dependencies
        run: dotnet restore --packages ${{ env.NUGET_PACKAGES_DIRECTORY }}

      - name: Build
        run: dotnet build ${{ env.PROJECT_PATH }} --configuration Release --no-restore --packages ${{ env.NUGET_PACKAGES_DIRECTORY }}

      # - name: Test
        # run: |
          # dotnet restore ${{ env.PROJECT_PATH }} --packages ${{ env.NUGET_PACKAGES_DIRECTORY }}
          # dotnet build ${{ env.PROJECT_PATH }} --configuration Release --no-restore
          # dotnet test ${{ env.TEST_PROJECT }} --configuration Release --no-build

  # test:
    # runs-on: [self-hosted, windows]
    # needs: build

    # steps:
      # - name: Checkout repository
        # uses: actions/checkout@v4
        # with:
          # clean: true
    
      # - name: Setup dotnet
        # uses: actions/setup-dotnet@v4
        # with:
          # dotnet-version: ${{ env.DOTNET_VERSION }}
        # env:
          # DOTNET_INSTALL_DIR: ${{ runner.tool_cache }}\dotnet

      # - name: Restore dependencies
        # run: dotnet restore --packages ${{ env.NUGET_PACKAGES_DIRECTORY }}

      # - name: Build
        # run: dotnet build ${{ env.TEST_PROJECT }} --configuration Release --no-restore --packages ${{ env.NUGET_PACKAGES_DIRECTORY }}

      # - name: Test
        # run: |
          # dotnet test ${{ env.TEST_PROJECT }} --configuration Release --no-build

  deploy_production:
    runs-on: [self-hosted, windows]
    needs: build
    environment: production
    env:
      IIS_DEPLOYMENT_PATH: C:\rehau\services\dev\REH.KFM

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          clean: true
    
      - name: Setup dotnet
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}
        env:
          DOTNET_INSTALL_DIR: ${{ runner.tool_cache }}\dotnet

      - name: Restore dependencies
        run: dotnet restore --packages ${{ env.NUGET_PACKAGES_DIRECTORY }}

      - name: Build
        run: dotnet build --configuration Release --no-restore --packages ${{ env.NUGET_PACKAGES_DIRECTORY }}

      - name: Test
        run: dotnet publish "REH.KFM.Service\REH.KFM.Service.csproj" -c release -o "${DeployPath}" /p:EnvironmentName=$Environment --no-build

  deploy_production2:
    runs-on: [self-hosted, windows]
    needs: build
    environment: production2

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          clean: true
    
      - name: Setup dotnet
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}
        env:
          DOTNET_INSTALL_DIR: ${{ runner.tool_cache }}/dotnet

      - name: Restore dependencies
        run: dotnet restore --packages ${{ env.NUGET_PACKAGES_DIRECTORY }}

      - name: Build
        run: dotnet build --configuration Release --no-restore --packages ${{ env.NUGET_PACKAGES_DIRECTORY }}

      - name: Test
        run: dotnet test --no-build